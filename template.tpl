___TERMS_OF_SERVICE___

By creating or modifying this file you agree to Google Tag Manager's Community
Template Gallery Developer Terms of Service available at
https://developers.google.com/tag-manager/gallery-tos (or such other URL as
Google may provide), as modified from time to time.


___INFO___

{
  "type": "TAG",
  "id": "cvt_temp_public_id",
  "version": 1,
  "securityGroups": [],
  "displayName": "tapp cookie CMP",
  "brand": {
    "id": "brand_dummy",
    "displayName": "TAPP Studio s.r.o.",
    "thumbnail": "data:image/png;base64,"
  },
  "description": "tapp cookie CMP for Consent Mode v2: default state (denied for new visitors), banner load, consent update after each choice. Docs: github.com/TAPP-STUDIO/gtm-template-tapp-cookie",
  "categories": [
    "UTILITY",
    "ANALYTICS",
    "ADVERTISING"
  ],
  "containerContexts": [
    "WEB"
  ]
}


___TEMPLATE_PARAMETERS___

[
  {
    "type": "TEXT",
    "name": "siteId",
    "displayName": "Site ID",
    "simpleValueType": true,
    "valueValidators": [
      { "type": "NON_EMPTY" },
      { "type": "REGEX", "args": ["^[a-z0-9][a-z0-9-]{1,48}$"] }
    ],
    "help": "The data-site value from the tapp cookie admin (Installation tab)."
  },
  {
    "type": "TEXT",
    "name": "tenantId",
    "displayName": "Tenant ID (account)",
    "simpleValueType": true,
    "valueValidators": [
      { "type": "NON_EMPTY" },
      { "type": "REGEX", "args": ["^[a-z0-9][a-z0-9-]{1,48}$"] }
    ],
    "help": "The data-tenant value from the tapp cookie admin (Installation tab)."
  },
  {
    "type": "TEXT",
    "name": "cmpUrl",
    "displayName": "CMP URL",
    "simpleValueType": true,
    "defaultValue": "https://cmp.tappcookie.cz",
    "valueValidators": [
      { "type": "NON_EMPTY" },
      { "type": "REGEX", "args": ["^https://[a-z0-9.-]+$"] }
    ],
    "help": "Without a trailing slash. The banner script is loaded from {CMP URL}/tappcookie.js. Leave the default unless tapp cookie support tells you otherwise."
  },
  {
    "type": "TEXT",
    "name": "locale",
    "displayName": "Banner language",
    "simpleValueType": true,
    "defaultValue": "",
    "help": "Empty = follow <html lang>. Otherwise a language code such as cs, en, sk or de."
  },
  {
    "type": "SELECT",
    "name": "blocking",
    "displayName": "Automatic script blocking",
    "macrosInSelect": false,
    "selectItems": [
      { "value": "auto", "displayValue": "On (recommended)" },
      { "value": "manual", "displayValue": "Off - the site marks its own scripts" }
    ],
    "help": "With blocking on, the banner holds known tracking scripts until consent. Note that scripts injected by GTM load asynchronously; gate those with Consent Mode in GTM.",
    "simpleValueType": true,
    "defaultValue": "auto"
  },
  {
    "type": "CHECKBOX",
    "name": "adsDataRedaction",
    "checkboxText": "ads_data_redaction (recommended)",
    "simpleValueType": true,
    "defaultValue": true
  },
  {
    "type": "CHECKBOX",
    "name": "urlPassthrough",
    "checkboxText": "url_passthrough",
    "simpleValueType": true,
    "defaultValue": false
  },
  {
    "type": "TEXT",
    "name": "waitForUpdate",
    "displayName": "wait_for_update (ms)",
    "simpleValueType": true,
    "defaultValue": 500,
    "valueValidators": [
      { "type": "POSITIVE_NUMBER" }
    ]
  },
  {
    "type": "SIMPLE_TABLE",
    "name": "regionalDefaults",
    "displayName": "Regional default state (optional)",
    "help": "For regions where the banner does not run (e.g. US without an opt-out rule) you can default to granted. Everywhere else stays denied. Region = ISO 3166-1 or ISO 3166-2 (US-CA).",
    "simpleTableColumns": [
      { "defaultValue": "", "displayName": "Regions (comma separated)", "name": "regions", "type": "TEXT" },
      {
        "defaultValue": "denied",
        "displayName": "ad_storage, ad_user_data, ad_personalization",
        "name": "ads",
        "type": "SELECT",
        "selectItems": [
          { "value": "granted", "displayValue": "granted" },
          { "value": "denied", "displayValue": "denied" }
        ]
      },
      {
        "defaultValue": "denied",
        "displayName": "analytics_storage",
        "name": "analytics",
        "type": "SELECT",
        "selectItems": [
          { "value": "granted", "displayValue": "granted" },
          { "value": "denied", "displayValue": "denied" }
        ]
      }
    ],
    "newRowButtonText": "Add region"
  }
]


___SANDBOXED_JS_FOR_WEB_TEMPLATE___

/*
 * tapp cookie CMP - GTM tag template.
 * 1. Consent Mode default from the first-party cookie `tapp_consent` when present, otherwise all
 *    denied - both with wait_for_update. The cookie only says "a decision exists"; its validity
 *    (TTL, declaration version) is checked by tappcookie.js, which sends an update to denied if
 *    the stored choice is no longer valid.
 * 2. injectScript tappcookie.js (GTM injects asynchronously, so synchronous auto-blocking is not
 *    possible from here; the recommended setup is to put the script directly in <head> and use
 *    this template for the consent signal only).
 * 3. Banner decisions (TappCookie.on("change")) -> updateConsentState + dataLayer event
 *    `tapp_consent_update`.
 */
const setDefaultConsentState = require('setDefaultConsentState');
const updateConsentState = require('updateConsentState');
const injectScript = require('injectScript');
const getCookieValues = require('getCookieValues');
const decodeUriComponent = require('decodeUriComponent');
const JSON = require('JSON');
const gtagSet = require('gtagSet');
const setInWindow = require('setInWindow');
const copyFromWindow = require('copyFromWindow');
const callInWindow = require('callInWindow');
const createQueue = require('createQueue');
const makeNumber = require('makeNumber');
const makeString = require('makeString');
const log = require('logToConsole');
const queryPermission = require('queryPermission');

const dataLayerPush = createQueue('dataLayer');

// Category -> signals (same as CATEGORY_TO_SIGNALS in @tapp/cookie-schema):
// necessary = security_storage; analytics = analytics_storage;
// marketing = ad_storage, ad_user_data, ad_personalization;
// preferences = functionality_storage, personalization_storage
function stateFromChoices(c) {
  const g = function (v) { return v ? 'granted' : 'denied'; };
  return {
    ad_storage: g(c.marketing),
    ad_user_data: g(c.marketing),
    ad_personalization: g(c.marketing),
    analytics_storage: g(c.analytics),
    functionality_storage: g(c.preferences),
    personalization_storage: g(c.preferences),
    security_storage: 'granted'
  };
}

// After a cookieDomain change the browser may hold more than one cookie. The SDK picks the
// one with the newest `at` (readNewestStoredCookie); the template must do the same, otherwise a
// month-old acceptance could override a fresh rejection.
function storedChoices() {
  const values = getCookieValues('tapp_consent');
  if (!values || values.length === 0) return null;
  // Only `forEach` and `const`: other constructs (for...of, let) have not been exercised in
  // the GTM sandbox with this template, so they are deliberately avoided.
  const best = { parsed: null, at: '' };
  values.forEach(function (raw) {
    const parsed = JSON.parse(decodeUriComponent(raw));
    if (!parsed || parsed.v !== 1 || !parsed.c) return;
    // `at` is an ISO string; the sandbox has no Date.parse, but string comparison of ISO dates is enough
    const at = makeString(parsed.at || '');
    if (!best.parsed || at > best.at) { best.parsed = parsed; best.at = at; }
  });
  if (!best.parsed) return null;
  return {
    analytics: best.parsed.c.analytics === true,
    marketing: best.parsed.c.marketing === true,
    preferences: best.parsed.c.preferences === true
  };
}

// 1) default
const stored = storedChoices();
const denied = {
  ad_storage: 'denied', ad_user_data: 'denied', ad_personalization: 'denied',
  analytics_storage: 'denied', functionality_storage: 'denied',
  personalization_storage: 'denied', security_storage: 'granted'
};
if (stored) {
  // `wait_for_update` belongs here too. The template does not know every validity rule the SDK
  // applies (ttlDays/rejectTtlDays expiry, a raised declarationVersion, a timestamp in the
  // future), so the stored choice may already be invalid and the SDK will shortly update it to
  // denied. Without the wait, tags would fire with granted consent before that correction.
  const fromCookie = stateFromChoices(stored);
  fromCookie.wait_for_update = makeNumber(data.waitForUpdate) || 500;
  setDefaultConsentState(fromCookie);
} else {
  const base = {};
  for (const k in denied) base[k] = denied[k];
  base.wait_for_update = makeNumber(data.waitForUpdate) || 500;
  // regional exceptions (granted only where the banner does not run)
  if (data.regionalDefaults && data.regionalDefaults.length) {
    data.regionalDefaults.forEach(function (row) {
      const regions = makeString(row.regions).split(',').map(function (r) { return r.trim(); }).filter(function (r) { return r; });
      if (!regions.length) return;
      const s = {};
      for (const k in denied) s[k] = denied[k];
      s.ad_storage = row.ads; s.ad_user_data = row.ads; s.ad_personalization = row.ads;
      s.analytics_storage = row.analytics;
      s.region = regions;
      s.wait_for_update = base.wait_for_update;
      setDefaultConsentState(s);
    });
  }
  setDefaultConsentState(base);
}
// `gtagSet` needs write_data_layer for every key it writes. If the permission were missing the
// call would throw - and since the callback registration, __tappCookieConfig and injectScript
// follow, the whole banner would silently be lost. So the permission is queried first and a
// mismatch is only logged; the Consent Mode default is already set and the banner still loads.
if (data.adsDataRedaction) {
  if (queryPermission('write_data_layer', 'ads_data_redaction')) gtagSet({ ads_data_redaction: true });
  else log('tapp cookie: missing write_data_layer permission for ads_data_redaction');
}
if (data.urlPassthrough) {
  if (queryPermission('write_data_layer', 'url_passthrough')) gtagSet({ url_passthrough: true });
  else log('tapp cookie: missing write_data_layer permission for url_passthrough');
}
// TODO (Google CMP Partner Program): once Google issues a developer ID, add
// gtagSet('developer_id.<id>', true) here (dot notation as in Google's documentation and
// published templates); the write_data_layer permission already covers developer_id.*.

// 3) listener for banner decisions: the sandbox has no addEventListener, so the SDK calls a
// callback registered through TappCookie.on("change").
const onConsent = function (choices, action) {
  if (!choices) return;
  updateConsentState(stateFromChoices(choices));
  dataLayerPush({
    event: 'tapp_consent_update',
    tapp_consent_action: action || 'update',
    tapp_consent_analytics: choices.analytics === true,
    tapp_consent_marketing: choices.marketing === true,
    tapp_consent_preferences: choices.preferences === true
  });
};
// 2) the script (if the site already has it in <head>, only the callback is registered)
const existing = copyFromWindow('TappCookie');
if (existing) {
  callInWindow('TappCookie.on', 'change', function (state) { onConsent(state.choices, 'update'); });
  data.gtmOnSuccess();
} else {
  // no regex literal here: the sandboxed JS parser rejects it ("token recognition error at: '\\'")
  const cmpBase = data.cmpUrl.charAt(data.cmpUrl.length - 1) === '/'
    ? data.cmpUrl.substring(0, data.cmpUrl.length - 1)
    : data.cmpUrl;
  const url = cmpBase + '/tappcookie.js';
  // injectScript cannot set data-* attributes; the SDK therefore also reads window.__tappCookieConfig
  setInWindow('__tappCookieConfig', {
    site: data.siteId, tenant: data.tenantId, cmp: data.cmpUrl,
    locale: data.locale || undefined, blocking: data.blocking
  }, true);
  if (!queryPermission('inject_script', url)) {
    // cmpUrl is a user field; an address outside the permitted hosts would otherwise fail silently
    log('tapp cookie: ' + url + ' is not among the origins this template may load from');
    data.gtmOnFailure();
    return;
  }
  injectScript(url, function () {
    callInWindow('TappCookie.on', 'change', function (state) { onConsent(state.choices, 'update'); });
    data.gtmOnSuccess();
  }, function () {
    log('tapp cookie: tappcookie.js failed to load, consent stays denied');
    data.gtmOnFailure();
  }, 'tappcookie');
}


___WEB_PERMISSIONS___

[
  {
    "instance": {
      "key": { "publicId": "access_consent", "versionId": "1" },
      "param": [
        {
          "key": "consentTypes",
          "value": {
            "type": 2,
            "listItem": [
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "ad_storage" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "ad_user_data" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "ad_personalization" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "analytics_storage" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "functionality_storage" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "personalization_storage" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "consentType" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }], "mapValue": [{ "type": 1, "string": "security_storage" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }] }
            ]
          }
        }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  },
  {
    "instance": {
      "key": { "publicId": "inject_script", "versionId": "1" },
      "param": [
        { "key": "urls", "value": { "type": 2, "listItem": [{ "type": 1, "string": "https://cmp.tappcookie.cz/" }] } }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  },
  {
    "instance": {
      "key": { "publicId": "get_cookies", "versionId": "1" },
      "param": [
        { "key": "cookieAccess", "value": { "type": 1, "string": "specific" } },
        { "key": "cookieNames", "value": { "type": 2, "listItem": [{ "type": 1, "string": "tapp_consent" }] } }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  },
  {
    "instance": {
      "key": { "publicId": "access_globals", "versionId": "1" },
      "param": [
        {
          "key": "keys",
          "value": {
            "type": 2,
            "listItem": [
              { "type": 3, "mapKey": [{ "type": 1, "string": "key" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }, { "type": 1, "string": "execute" }], "mapValue": [{ "type": 1, "string": "dataLayer" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }, { "type": 8, "boolean": false }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "key" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }, { "type": 1, "string": "execute" }], "mapValue": [{ "type": 1, "string": "TappCookie" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": false }, { "type": 8, "boolean": false }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "key" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }, { "type": 1, "string": "execute" }], "mapValue": [{ "type": 1, "string": "TappCookie.on" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": false }, { "type": 8, "boolean": true }] },
              { "type": 3, "mapKey": [{ "type": 1, "string": "key" }, { "type": 1, "string": "read" }, { "type": 1, "string": "write" }, { "type": 1, "string": "execute" }], "mapValue": [{ "type": 1, "string": "__tappCookieConfig" }, { "type": 8, "boolean": true }, { "type": 8, "boolean": true }, { "type": 8, "boolean": false }] }
            ]
          }
        }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  },
  {
    "instance": {
      "key": { "publicId": "write_data_layer", "versionId": "1" },
      "param": [
        { "key": "keyPatterns", "value": { "type": 2, "listItem": [{ "type": 1, "string": "ads_data_redaction" }, { "type": 1, "string": "url_passthrough" }, { "type": 1, "string": "developer_id.*" }, { "type": 1, "string": "event" }, { "type": 1, "string": "tapp_consent_action" }, { "type": 1, "string": "tapp_consent_analytics" }, { "type": 1, "string": "tapp_consent_marketing" }, { "type": 1, "string": "tapp_consent_preferences" }] } }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  },
  {
    "instance": {
      "key": { "publicId": "logging", "versionId": "1" },
      "param": [
        { "key": "environments", "value": { "type": 1, "string": "debug" } }
      ]
    },
    "clientAnnotations": { "isEditedByUser": true },
    "isRequired": true
  }
]


___TESTS___

scenarios:
- name: no cookie -> default denied + wait_for_update
  code: |-
    mock('getCookieValues', function () { return []; });
    mock('setDefaultConsentState', function (s) {
      assertThat(s.ad_storage).isEqualTo('denied');
      assertThat(s.analytics_storage).isEqualTo('denied');
      assertThat(s.security_storage).isEqualTo('granted');
      assertThat(s.wait_for_update).isEqualTo(500);
    });
    mock('injectScript', function (url, ok) { ok(); });
    runCode({ siteId: 'web', tenantId: 'klient', cmpUrl: 'https://cmp.tappcookie.cz', waitForUpdate: 500, adsDataRedaction: true, blocking: 'auto' });
    assertApi('gtmOnSuccess').wasCalled();
- name: valid cookie -> default from the stored choice
  code: |-
    mock('getCookieValues', function () { return ['%7B%22v%22%3A1%2C%22id%22%3A%22x%22%2C%22at%22%3A%222026-01-01T00%3A00%3A00.000Z%22%2C%22dv%22%3A1%2C%22c%22%3A%7B%22analytics%22%3Atrue%2C%22marketing%22%3Afalse%2C%22preferences%22%3Afalse%7D%7D']; });
    mock('setDefaultConsentState', function (s) {
      assertThat(s.analytics_storage).isEqualTo('granted');
      assertThat(s.ad_storage).isEqualTo('denied');
      assertThat(s.wait_for_update).isEqualTo(500);
    });
    mock('injectScript', function (url, ok) { ok(); });
    runCode({ siteId: 'web', tenantId: 'klient', cmpUrl: 'https://cmp.tappcookie.cz', waitForUpdate: 500, blocking: 'auto' });
    assertApi('gtmOnSuccess').wasCalled();


___NOTES___

Created 2026-08-21 by TAPP Studio s.r.o. (https://tappcookie.cz). Source and issues: https://github.com/TAPP-STUDIO/gtm-template-tapp-cookie
